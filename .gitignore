node_modules/
dist/
# Los datos generados (public/data y public/tiles/*.pmtiles) SÍ se versionan: el deploy los necesita.
# Formato anterior de tiles (un archivo por tile), ya no se usa:
public/tiles/*/
*.part

# Configuración local de herramientas
.claude/

# Variables locales con secretos (no versionar)
.env.local
.env.*.local
